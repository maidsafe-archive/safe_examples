/**
* Manage the Request
* TODO MAID-1463
*/
var RequestManager = function(encryptionKey, connectionManager) {

  this.send = function(data, callback) {
    // TODO Encrypt and send the data
    // connectionManager.send
  }

};

exports = module.exports = RequestManager();
